'use client';

export default function Footer() {
  return (
    <footer className="w-full border-t border-white/10 bg-[rgb(var(--surf-1))]/80 backdrop-blur-xl px-4 py-5 text-center text-xs text-white/50">
      <p>SyncMusic © 2026. Built for real-time synchronized listening.</p>
    </footer>
  );
}
